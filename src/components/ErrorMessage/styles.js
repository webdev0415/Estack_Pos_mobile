import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  error: {
    width: 245,
    fontSize: 10,
    color: 'red',
    marginBottom: -12,
    paddingTop: 2,
  },
});

export default styles;
